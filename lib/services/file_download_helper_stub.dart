import 'package:flutter/foundation.dart';

void downloadBytesHelper(Uint8List bytes, String filename) {
  debugPrint('[FileDownloadHelper] Non-web download stub for filename: $filename (${bytes.length} bytes)');
}

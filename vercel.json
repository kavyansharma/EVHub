{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "( [ -d flutter ] || git clone https://github.com/flutter/flutter.git -b stable --depth 1 ) && ./flutter/bin/flutter config --enable-web",
  "buildCommand": "./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
